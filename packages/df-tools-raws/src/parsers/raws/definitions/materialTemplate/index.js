import {MATERIAL_TEMPLATE} from '../generatedDefinitions';
import {withChildren} from '../definitionUtils';
import materialTemplate from '../shared/materialTemplate';

export default withChildren(MATERIAL_TEMPLATE, materialTemplate);
