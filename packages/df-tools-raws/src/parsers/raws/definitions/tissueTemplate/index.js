import {TISSUE_TEMPLATE} from '../generatedDefinitions';
import {withChildren} from '../definitionUtils';
import tissueTemplate from '../shared/tissueTemplate';

export default withChildren(TISSUE_TEMPLATE, tissueTemplate);
