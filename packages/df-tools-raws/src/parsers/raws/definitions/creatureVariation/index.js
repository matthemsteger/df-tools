import {CREATURE_VARIATION} from '../generatedDefinitions';
import creatureVariations from '../shared/creatureVariation';
import {withChildren} from '../definitionUtils';

export default withChildren(CREATURE_VARIATION, creatureVariations);
